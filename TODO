[ X ] Create foundation type which will contain metadata information about structure fields, methods, etc.
[ X ] Rework compiler to compile output from parser and not from lexer.
[ X ] Create tuple implementation.